## Summary

<!-- What this PR delivers and which milestone in docs/project-roadmap.md it belongs to.
     Two or three sentences, written for someone who has not read the code. -->

## Evidence

<!-- Show it working. Screenshot, screen recording, or terminal output —
     whatever proves the behaviour fastest for this change.
     Endpoints: request and response. UI-less changes: the command and its output. -->

## Changes

<!-- Grouped by area, not file by file. For example:
       - Prisma: datasource, PrismaService lifecycle, generated client wiring
       - i18n: en/vi locales, Accept-Language resolver
     For PRs that add endpoints, list method, path and auth requirement. -->

## Design notes

<!-- Decisions a reviewer would otherwise have to ask about:
       - anything that departs from the training guideline, and why
       - alternatives considered and rejected, in one line each
       - trade-offs knowingly accepted
     Omit the section if the PR is routine. -->

## Deferred

<!-- What was deliberately left out and which milestone it lands in, so an
     intentional omission is not reported as a defect. -->
