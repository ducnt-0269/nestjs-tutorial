// The message half of the sentence that §4 of the architecture spells out: a
// key, then one of these. Only the wordings more than one module types out live
// here; the rest sit at their single use site, next to the body they fill.
export const blank = "can't be blank";
export const invalid = 'is invalid';
export const taken = 'has already been taken';
